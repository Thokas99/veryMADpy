[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "veryMADpy"
version = "0.0.1"
description = "Explicit MAD quality-control annotation for Python and scverse"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Thomas Sirchi", email = "thomasirchi@gmail.com" }]
keywords = ["bioinformatics", "scanpy", "anndata", "quality-control", "robust-statistics"]
dependencies = ["numpy", "pandas", "anndata"]

[project.urls]
Homepage = "https://github.com/Thokas99/veryMADpy"
Repository = "https://github.com/Thokas99/veryMADpy"
Issues = "https://github.com/Thokas99/veryMADpy/issues"
Changelog = "https://github.com/Thokas99/veryMADpy/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = ["pytest", "pytest-cov", "ruff"]
test-scanpy = ["scanpy"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
